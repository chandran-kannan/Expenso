import { useState } from "react";
import RoutesWrapper from "./Routes";

const App = () => {
  
  return (
    <div className="h-full">
      <RoutesWrapper />
    </div>
  );
};

export default App;
